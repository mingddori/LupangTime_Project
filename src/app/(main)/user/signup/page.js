import { Grid2 as Grid, } from "@mui/material";

import SignUpComponent from "@/app/(main)/_component/SignUpComponent";

export default function SignUpPage() {
    return (
        <Grid size={12}>
            <SignUpComponent />
        </Grid>
    )
}