import { Button, Grid2 as Grid, Paper, Stack, Typography } from "@mui/material";

export default function LoginPage() {
    return (
        <Grid container size={12} sx={{ height: "100%", display: "flex", flexDirection: "column", background : "red" }}>
            dd
        </Grid>
    )
}